## inScript Connect Bakend Server

This is the server for inScript

Use this when you want to run an environment variable automatically

"spec-dev": "NODE_ENV=ProUser ts-node src/bin/server.ts | bunyan -o short"
